<% teams = Team.all%>
<% teams.each do |team| %>
    <div class="col-12 col-md-6 mt-4">
        <div class="card shadow-sm">
            <div class="card-body">
                <p class="subtitle card-text" style="font-weight: bold;"><%= team.name %></p>
                <table class="table">
                    <thead>
                        <th scope="col">Nombre</th>
                        <th scope="col">Framework</th>
                    </thead>
                    <tbody>
                        <% team.users.each do |user| %>
                            <tr>
                                <td><%= user.full_name %></td>
                                <td><%= user.framework %></td>
                                <% if user.has_role? :captain%>
                                    <td>Capitan</td>
                                <% end %>
                            </tr>
                        <% end %>
                </table>
                <% if team.team_invitations.where(member_id: current_user.member.id).present?%>
                    <div class="alert alert-warning">
                        <strong>Ya enviaste una invitación a este equipo</strong>
                    </div>

                <%elsif team.members.count >= 3%>
                    <div class="alert alert-danger">
                        <strong>¡Lo sentimos!</strong>
                        <p>El equipo está lleno.</p>
                    </div>
                <%else%>
                    <button onclick="sendInvitation(<%= team.id %>)" class="btn btn-primary">Unirme</button>                
                <%end%>
                
            </div>
        </div>
    </div>
<%end%>
<script>
    function sendInvitation(team_id) {
        let data = {
            team_invitation: {
                team_id: team_id,
                member_id: <%= current_user.member.id %>
            }
        }
        fetch('/team_invitations', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
        })
        window.location.replace("<%= root_path %>");

    }

</script>





